{
    "typescript.tsdk": "node_modules\\typescript\\lib",
    "triggerTaskOnSave.tasks": {
        "restart backend": ["src/**/*"]
        // everytime we make changes to the 'src' directory, it will trigger the restart backend
    }
}